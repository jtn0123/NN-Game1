# Compatibility shim for the historical setup command:
#     pip install -r requirements.txt
#
# Project dependencies now live in pyproject.toml. This installs the app with
# web, test, and dev extras so old setup instructions still produce a complete
# local development environment.

-e .[web,test,dev]
